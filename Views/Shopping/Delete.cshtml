@model simple_stock_manager.Models.Shopping

@{
    ViewData["Title"] = "Deletar compra";
}

<h1>Deletar</h1>
<h4>Compra</h4>
<hr>

<h6 class="alert alert-danger">Você tem certeza que disso?</h6>
<div>
    <dl class="row">
        <dt class = "col-sm-2">Cliente</dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Customer.Name)
        </dd class>
        <dt class = "col-sm-2">Produto</dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Product.Name)
        </dd class>
    </dl>
    <hr />
    <form asp-action="Delete">
        <div class="d-inline p-2">
            <button type="" value="Salvar" class="btn btn-danger">
                <i class="fas fa-trash"></i> Deletar
            </button>
        </div>
        <div class="d-inline p-2">
            <a asp-action="Index" class="btn btn-secondary float-right">
                <i class="fas fa-arrow-circle-left"></i> Voltar
            </a>
        </div>
    </form>
</div>
